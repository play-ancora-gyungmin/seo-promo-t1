---
import type { LandingSectionKind } from '../../types/landing';

export interface Props {
  id?: string;
  kind?: LandingSectionKind;
  class?: string;
  surfaceClass?: string;
  noSurface?: boolean;
}

const {
  id,
  kind = 'flow',
  class: className = '',
  surfaceClass = '',
  noSurface = false
} = Astro.props;
---

<section
  id={id}
  class:list={['landing-section', `landing-section--${kind}`, className]}
  data-slide-section={kind === 'slide' ? '' : undefined}
  data-section-kind={kind}
>
  <div
    class:list={[
      'landing-section__surface',
      noSurface && 'landing-section__surface--bare',
      surfaceClass
    ]}
  >
    <div class:list={['landing-section__shell', !noSurface && 'section-shell']}>
      <slot />
    </div>
  </div>
</section>
